services:
  - type: web
    name: meteor-space-dodge-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: METEOR_DODGE_DB_URL
        sync: false
